export type product = {
    id: number,
    name: string,
    price: string,
    category: string,
    store: string,
    description: string,
    barcode: string,
    image_url: string,
}

export type user = {
    id: number,
    first_name: string,
    last_name: string,
    email: string,
}